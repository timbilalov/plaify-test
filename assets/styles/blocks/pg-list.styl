.pg-list
    position fixed
    left (site-paddings / 2)
    bottom rel-size(30px)
    z-index 2
    width rel-size(40px)
    height rel-size(40px)
    line-height rel-size(38px)
    border-radius 100%
    border rel-size(1px) solid lighten(black, 80)
    background-color white
    text-align center

    img
        display inline-block
        vertical-align middle
        width 50%
        height auto

    &--2
        bottom rel-size(80px)

    &--3
        bottom rel-size(130px)