{
  // Default options
  'functions': true,
  'variableNameTransforms': ['underscored']
}

{{#block "spritesheet"}}
${{spritesheet.name}}-sprite
    background-image url('{{{spritesheet.escaped_image}}}')

{{#block "sprites"}}
{{#each sprites}}
{{name}}--{{../spritesheet.name}} = {{px.x}} {{px.y}} {{px.offset_x}} {{px.offset_y}} {{px.width}} {{px.height}} {{px.total_width}} {{px.total_height}}
{{/each}}
{{/block}}


{{/block}}